export const PAYMENT_SELECT_COLUMNS = `
  id,
  sender_user_id,
  sender_wallet,
  sender_phone_identity_pubkey,
  sender_display_name_snapshot,
  sender_handle_snapshot,
  reference_code,
  receiver_phone,
  receiver_phone_hash,
  payment_mode,
  sender_autoclaim_enabled,
  receiver_autoclaim_allowed,
  receiver_wallet,
  receiver_onboarded,
  phone_identity_pubkey,
  payment_receiver_pubkey,
  ephemeral_pubkey,
  refund_receiver_pubkey,
  refund_ephemeral_pubkey,
  token_symbol,
  token_mint_address,
  amount,
  sender_fee_amount,
  claim_fee_amount,
  escrow_account,
  escrow_vault_address,
  deposit_signature,
  release_signature,
  refund_release_signature,
  released_to_wallet,
  refund_released_to_wallet,
  refund_requested_at,
  refund_available_at,
  refund_claimed_at,
  expiry_at,
  notification_message_id,
  notification_status,
  notification_sent_at,
  notification_delivered_at,
  notification_read_at,
  notification_failed_at,
  notification_attempt_count,
  notification_last_attempt_at,
  status,
  created_at
`;
